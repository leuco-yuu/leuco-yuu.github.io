# 全书统一公式模板

## 概率与优势

```latex
\Pr\bigl[\mathsf{Exp}_{\Pi,\mathcal A}^{\mathsf{goal}}(1^\lambda)=1\bigr]
```

```latex
\operatorname{Adv}_{\Pi,\mathcal A}^{\mathsf{IND\text{-}CCA}}(\lambda)
:=\left|\Pr[b'=b]-\frac12\right|.
```

```latex
\Delta(X,Y)
:=\frac12\sum_{x}\left|\Pr[X=x]-\Pr[Y=x]\right|.
```

## LWE 与 MLWE

```latex
\mathbf A\xleftarrow{\$}\mathbb Z_q^{n\times m},\quad
\mathbf s\leftarrow\chi_s^n,\quad
\mathbf e\leftarrow\chi_e^m,\quad
\mathbf b:=\mathbf A^{\top}\mathbf s+\mathbf e\pmod q.
```

```latex
\mathbf A\xleftarrow{\$}R_q^{k\times\ell},\quad
\mathbf s\leftarrow\chi_s^{\ell},\quad
\mathbf e\leftarrow\chi_e^k,\quad
\mathbf b:=\mathbf A\mathbf s+\mathbf e.
```

## SIS 与预像

```latex
\mathbf z\ne\mathbf0,\qquad
\mathbf A\mathbf z=\mathbf0\pmod q,\qquad
\|\mathbf z\|_2\le\beta_{\rm SIS}.
```

```latex
\mathbf x\leftarrow
D_{\Lambda_q^{\mathbf u}(\mathbf A),s_{\rm pre}},
\qquad \mathbf A\mathbf x=\mathbf u\pmod q.
```

## 正确性

```latex
\Pr\left[
\mathsf{Dec}(\mathsf{sk},
\mathsf{Enc}(\mathsf{pk},\mu))\ne\mu
\right]\le p_{\rm fail}(\lambda).
```

```latex
\mathsf{Fail}_{\rm dec}
:=\left\{\|\mathbf E_{\rm dec}\|_{\infty}\ge T_{\rm dec}\right\}.
```

## KEM

```latex
(\mathsf{pk},\mathsf{sk})\leftarrow\mathsf{KeyGen}(1^\lambda),\qquad
(\mathsf{ct},\mathsf K)\leftarrow\mathsf{Encaps}(\mathsf{pk}),\qquad
\mathsf K'\leftarrow\mathsf{Decaps}(\mathsf{sk},\mathsf{ct}).
```

## 签名

```latex
(\mathsf{pk},\mathsf{sk})\leftarrow\mathsf{KeyGen}(1^\lambda),\qquad
\sigma_{\rm sig}\leftarrow\mathsf{Sign}(\mathsf{sk},\mu),\qquad
\mathsf{Verify}(\mathsf{pk},\mu,\sigma_{\rm sig})=1.
```

## 零知识

```latex
\mathcal R(x,w)=1,
\qquad
\mathsf{tr}:=(\mathsf{com},c,z),
\qquad
\mathsf{VerifyProof}(x,\pi_{\rm zk})=1.
```

## 多接收者 mmKEM

```latex
\mathsf{CT}
:=\left(\mathsf{hdr},
\{(\mathsf{id}_i,\mathsf{ct}_i)\}_{i=1}^{N}\right),
\qquad
\mathsf K_i
:=\mathsf{Decaps}_i(\mathsf{sk}_i,\mathsf{CT}).
```

---

