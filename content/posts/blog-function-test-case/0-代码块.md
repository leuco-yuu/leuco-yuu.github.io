# 代码

## 常规代码块

本博客对 Hugo 默认代码块做了大量定制，支持以下功能：

- **语法高亮**：使用 Hugo 内置 Chroma 引擎，风格为 `github`，支持几乎所有主流语言（Python、Java、C、Go、HTML、YAML 等）；
- **语言标签**：代码框左上角自动显示语言名称（大写）；
- **语言元信息（元数据）**：可通过 `filename=` 属性自定义标签名；
- **复制按钮**：右上角有复制按钮，点击后显示"已复制！"反馈；
- **折叠/展开按钮**：右上角有折叠按钮；
- **自动折叠**：超过 30 行或 400px 高度的代码框会自动折叠（可在 `hugo.yaml` → `params.codeblock.collapse` 调整参数）；
- **折叠浮层**：折叠后底部显示半透明渐变遮罩 + 居中的展开按钮，悬停可见
- **Hover 动效**：代码框整体有 hover 上浮 + 阴影增强效果。

---

**格式案例 1**：

````markdown
```python
print("Hello World!")
```
````

**渲染样式**：

```python
print("Hello World!")
```

---

**格式案例 2**：

````markdown
```python {filename="Hello.py"}
print("Hello World!")
```
````

**渲染样式**：

```python {filename="Hello.py"}
print("Hello World!")
```

---

## Output代码

特殊类型代码框，用 `output` 作为语言标识，专门用于展示程序输出结果：

- **折叠箭头**：头部显示收起/展开箭头，默认折叠；
- **无语法高亮**：输出文本黑白显示，无语法着色（作为特殊输出效果）；
- **自动合并**：当 output 代码框紧跟在常规代码框之后时，二者边框会融为一体，展现为"代码+输出"的连续视觉效果。

---

**格式案例 1**:

````markdown
```output
W_hat:
[[5.02671056 3.98404888 4.00452019 4.9783491  1.02490084]]
X_pre: 
[[0.03016482 4.79096225 5.74410225 4.2038564 ]
 [0.0650456  9.53496505 3.37288732 7.77475153]
 [2.68648726 5.44484467 6.07293252 8.00564199]]
Y_pre: 
[[57.23707594]
 [49.07109113]
 [83.7887313 ]]
```
````

**渲染样式**:

```output
W_hat:
[[5.02671056 3.98404888 4.00452019 4.9783491  1.02490084]]
X_pre: 
[[0.03016482 4.79096225 5.74410225 4.2038564 ]
 [0.0650456  9.53496505 3.37288732 7.77475153]
 [2.68648726 5.44484467 6.07293252 8.00564199]]
Y_pre: 
[[57.23707594]
 [49.07109113]
 [83.7887313 ]]
```

---

**格式案例 2**：

````markdown
```python
# %%
# 这里是线性回归 梯度下降参数估计过程
import numpy as np

def gradient(X, Y, W, n):
    y_hat = X.dot(W)
    error = y_hat - Y
    grad = X.T.dot(error) 
    return grad

def loss(X, Y, W, n):
    y_hat = X.dot(W)
    error = y_hat - Y
    return np.sum(error ** 2) / n
```

```output
W_hat:
[[5.02671056 3.98404888 4.00452019 4.9783491  1.02490084]]
X_pre: 
[[0.03016482 4.79096225 5.74410225 4.2038564 ]
 [0.0650456  9.53496505 3.37288732 7.77475153]
 [2.68648726 5.44484467 6.07293252 8.00564199]]
Y_pre: 
[[57.23707594]
 [49.07109113]
 [83.7887313 ]]
```
````

**渲染样式**:

```python
# %%
# 这里是线性回归 梯度下降参数估计过程
import numpy as np

def gradient(X, Y, W, n):
    y_hat = X.dot(W)
    error = y_hat - Y
    grad = X.T.dot(error) 
    return grad

def loss(X, Y, W, n):
    y_hat = X.dot(W)
    error = y_hat - Y
    return np.sum(error ** 2) / n
```

```output
W_hat:
[[5.02671056 3.98404888 4.00452019 4.9783491  1.02490084]]
X_pre: 
[[0.03016482 4.79096225 5.74410225 4.2038564 ]
 [0.0650456  9.53496505 3.37288732 7.77475153]
 [2.68648726 5.44484467 6.07293252 8.00564199]]
Y_pre: 
[[57.23707594]
 [49.07109113]
 [83.7887313 ]]
```

---

## 常规代码块

除标准语言外的特性：

- **带语言名称**：```` ```go ```` → 显示 `GO` 标签
- **输出代码块**：```` ```output ```` → 显示 `OUTPUT` 标签，无高亮，背景深暗
- **忽略未知语言**：```` ```unknown ```` → 显示 `UNKNOWN` 标签

### 案例: 展示 Go 和 Output 代码块

**格式**：

````markdown
```go
package main
import "fmt"
func main() { fmt.Println("Hello, Narrow!") }
```

```output
Hello, Narrow!
```
````

**渲染**：

```go
package main
import "fmt"
func main() { fmt.Println("Hello, Narrow!") }
```

```output
Hello, Narrow!
```
