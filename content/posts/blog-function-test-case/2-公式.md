# 公式渲染

本博客项目已集成 KaTeX，支持行内和块级数学公式：

- **行内公式**：`$...$` 或 `\(...\)`；
- **块级公式**：`$$...$$` 或 `\[...\]`；
- **复制支持**：通过集成了 `copy-tex` 插件，点击公式可复制完整 LaTeX 源码；
- **自动检测**：页面有数学内容时自动加载 KaTeX 资源。

```demo {title="Latex 公式渲染",render="md"}
$$
e^{i\theta} = \cos\theta + i\sin\theta
$$
```
